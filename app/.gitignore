# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Environment variables
.env
.env.local
.env.*.local
# Database
*.db
*.sqlite
*.sqlite3
db/migrations/*.sql
# TypeScript cache
*.tsbuildinfo
# Build outputs
dist/
build/
# Vercel local config
.vercel/
# NOTE: api/index.mjs IS committed. Vercel auto-discovers functions from
# source files BEFORE running buildCommand, so the bundle must be in git.
# Regenerate with `npm run vercel-build` whenever _handler.ts, _boot.ts,
# or any transitively imported source (api/**, contracts/**, db/**) changes.
# Test coverage
coverage/
.nyc_output/
